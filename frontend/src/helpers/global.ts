export enum Global {
    BASE_URL = "http://localhost:3000",
    CATEGORIES = "categories",
    PRODUCTS = "products",
    ORDERS = "orders",
    LOGIN = "login",
    BY_STATUS = "selectBy/status",
    STATUS = "status",
}